# Быстрый запуск Асси

Асси — локальный ИИ-ассистент для поиска и ответов по загруженным учебным медицинским материалам.

Самый простой способ запуска — через Docker Compose. При таком варианте **не нужно отдельно устанавливать Python, PostgreSQL, Qdrant, Node.js или ML-модели**: Docker создаст и настроит всё необходимое.

## Что будет запущено

| Компонент | Адрес | Назначение |
|---|---|---|
| Web-интерфейс | `http://127.0.0.1:3000` | Загрузка материалов, поиск и вопросы |
| FastAPI | `http://127.0.0.1:8000` | Backend API |
| Swagger UI | `http://127.0.0.1:8000/docs` | Интерактивная документация API |
| Healthcheck | `http://127.0.0.1:8000/api/v1/health` | Проверка состояния сервисов |
| PostgreSQL | `localhost:5432` | Документы, задания индексации и feedback |
| Qdrant | `http://127.0.0.1:6333` | Векторный индекс фрагментов |

## 1. Подготовь компьютер

Нужны только:

- Docker Desktop с Docker Compose;
- Git — либо скачанный ZIP-архив репозитория.

Подробные инструкции для компьютера, на котором ничего не установлено:

- [Установка на macOS](install_macos.md);
- [Установка на Windows](install_windows.md).

## 2. Скачай проект

### macOS — Terminal

```bash
git clone https://github.com/ssd2008/MLproject.git
cd MLproject
```

### Windows — PowerShell

```powershell
git clone https://github.com/ssd2008/MLproject.git
Set-Location MLproject
```

Если Git не установлен, скачай репозиторий через GitHub: **Code → Download ZIP**, распакуй архив и открой терминал в полученной папке.

> Репозиторий должен быть доступен твоему GitHub-аккаунту. Для приватного репозитория Git может запросить авторизацию.

## 3. Запусти приложение

Команда одинакова на macOS и Windows:

```bash
docker compose up --build
```

Во время первого запуска Docker:

1. скачает образы PostgreSQL и Qdrant;
2. соберёт backend и frontend;
3. установит Python-зависимости;
4. скачает embedding-модель, reranker и Whisper-модель;
5. применит SQL-миграции;
6. запустит приложение.

Первый запуск заметно дольше последующих и требует нескольких гигабайт свободного места. Не закрывай Docker Desktop и терминал, пока сервисы не перейдут в рабочее состояние.

Проверить состояние в другом терминале:

```bash
docker compose ps
```

Посмотреть ключевые логи:

```bash
docker compose logs -f model-init api frontend
```

## 4. Открой Асси

Открой в браузере:

```text
http://127.0.0.1:3000
```

<!-- TODO: вставить скрин главной страницы Асси -->

Проверка API:

### macOS

```bash
curl http://127.0.0.1:8000/api/v1/health
```

### Windows PowerShell

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/v1/health
```

Ожидаемый общий статус:

```json
{
  "status": "ok"
}
```

Фактический ответ также содержит версии и состояния PostgreSQL, Qdrant, embedding-, reranker-, ASR- и answer-backend.

## 5. Проверь основной сценарий

1. Загрузи PDF, видео, текст или URL.
2. Запусти индексацию материала.
3. Дождись статуса `Готов`.
4. Выполни поиск по содержимому.
5. Задай вопрос Асси.
6. Проверь цитаты, страницы PDF или тайм-коды видео.

<!-- TODO: вставить скрин загрузки документа -->
<!-- TODO: вставить скрин результатов поиска -->
<!-- TODO: вставить скрин ответа с цитатами -->

## Остановка и повторный запуск

Остановить приложение:

```bash
docker compose down
```

Запустить повторно без пересборки:

```bash
docker compose up
```

Запустить в фоне:

```bash
docker compose up -d
```

Посмотреть логи фонового запуска:

```bash
docker compose logs -f
```

## Полный сброс данных

```bash
docker compose down -v
```

> **Осторожно:** параметр `-v` удаляет Docker volumes — тома с PostgreSQL, Qdrant, загруженными файлами и кэшем моделей. После этого данные будут потеряны, а модели придётся скачивать заново.

## Что читать дальше

- [Полное описание проекта](description.md);
- [API-спецификация](api_spec.md);
- [Локальная разработка](development.md);
- [Типовые проблемы](troubleshooting.md).
