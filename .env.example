APP_ENV=local
DEBUG=false
LOG_LEVEL=INFO
DATABASE_URL=postgresql://med_user:med_pass@localhost:5432/med_assistant
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=document_chunks_e5_large_v1
MAX_DOCUMENT_SIZE_MB=10

# Real retrieval models used by the Docker Compose stack.
EMBEDDING_BACKEND=sentence-transformers
EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-large
EMBEDDING_DIMENSION=1024
EMBEDDING_BATCH_SIZE=8
EMBEDDING_DEVICE=cpu
NORMALIZE_EMBEDDINGS=true

RERANKER_BACKEND=cross-encoder
RERANKER_MODEL_NAME=BAAI/bge-reranker-v2-m3
RERANKER_BATCH_SIZE=4
RERANKER_DEVICE=cpu

# The answer is assembled only from retrieved source fragments. This mode does
# not require an external LLM or an API key.
ANSWER_BACKEND=extractive

# Lightweight options for tests or machines where PyTorch is unavailable:
# EMBEDDING_BACKEND=hash
# RERANKER_BACKEND=lexical
# QDRANT_COLLECTION_NAME=document_chunks_v1

# Optional generative answers:
# ANSWER_BACKEND=openai
# OPENAI_API_KEY=...
# OPENAI_MODEL=gpt-4.1-mini
